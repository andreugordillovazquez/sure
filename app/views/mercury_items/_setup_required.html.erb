<%= turbo_frame_tag "modal" do %>
  <%= render DS::Dialog.new do |dialog| %>
    <% dialog.with_header(title: t("mercury_items.setup_required.title")) %>
    <% dialog.with_body do %>
      <div class="space-y-4">
        <div class="flex items-start gap-3">
          <%= icon("alert-circle", class: "text-warning w-5 h-5 shrink-0 mt-0.5") %>
          <div class="text-sm text-secondary">
            <p class="font-medium text-primary mb-2"><%= t("mercury_items.setup_required.api_token_not_configured") %></p>
            <p><%= t("mercury_items.setup_required.configure_api_token") %></p>
          </div>
        </div>

        <div class="bg-surface rounded-lg p-4 space-y-2 text-sm">
          <p class="font-medium text-primary"><%= t("mercury_items.setup_required.setup_steps") %></p>
          <ol class="list-decimal list-inside space-y-1 text-secondary">
            <li><%= t("mercury_items.setup_required.step_1") %></li>
            <li><%= t("mercury_items.setup_required.step_2") %></li>
            <li><%= t("mercury_items.setup_required.step_3") %></li>
            <li><%= t("mercury_items.setup_required.step_4") %></li>
          </ol>
        </div>

        <div class="mt-4">
          <%= link_to settings_providers_path,
                      class: "inline-flex items-center justify-center rounded-lg px-4 py-2 text-sm font-medium text-white bg-gray-900 hover:bg-gray-800 focus:outline-none focus:ring-2 focus:ring-gray-900 focus:ring-offset-2 transition-colors",
                      data: { turbo: false } do %>
            <%= t("mercury_items.setup_required.go_to_provider_settings") %>
          <% end %>
        </div>
      </div>
    <% end %>
  <% end %>
<% end %>
